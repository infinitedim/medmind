enum Mood { great, good, okay, bad, terrible }

enum ActivityLevel { sedentary, light, moderate, active }

enum SymptomCategory {
  neurological,
  digestive,
  respiratory,
  musculoskeletal,
  psychological,
  skin,
  general,
}

enum FactorType { boolean, numeric, scale }

enum InsightType { correlation, anomaly, trend, recommendation }

enum ScoreTrend { improving, stable, declining }
