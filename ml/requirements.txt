# ============================================================
# MEDMIND — ML Requirements
# Sesuai dengan 3 model: NLP Symptom, Anomaly Detection, Correlation
# Python 3.10+ recommended
# ============================================================

# ============================
# Core TensorFlow & TFLite
# (sesuai tflite_flutter di pubspec)
# ============================
tensorflow==2.16.1
tensorflow-lite-support==0.4.4       # TFLite metadata & post-processing
flatbuffers==23.5.26                  # TFLite model format dependency

# ============================
# NLP — nlp_symptom_v1.tflite
# (untuk symptom_classifier.dart)
# ============================
transformers==4.40.0                  # BERT/MobileBERT fine-tuning
tokenizers==0.19.1                    # Fast tokenizer (pair dengan transformers)
datasets==2.19.0                      # Load health NLP datasets dari HuggingFace
nltk==3.8.1                           # Rule-based tokenizer & fallback
sentence-transformers==3.0.0          # Semantic similarity antar symptom
tf2onnx==1.16.1                       # TF → ONNX → TFLite pipeline (opsional)

# ============================
# Anomaly Detection — anomaly_detection_v1.tflite
# (untuk anomaly_model.dart)
# ============================
scikit-learn==1.4.2                   # IsolationForest, preprocessing, z-score
pyod==1.1.3                           # Python Outlier Detection library

# Correlation — symptom_correlation_v1.tflite
# (untuk correlation_model.dart)
scipy==1.13.0                         # Pearson, Spearman, chi-square, p-values
statsmodels==0.14.2                   # Lagged correlation, time-series analysis
pingouin==0.5.4                       # Bonferroni correction, effect sizes

# Data Processing
numpy==1.26.4
pandas==2.2.2
imbalanced-learn==0.12.2

# Indonesian NLP Support
PySastrawi==1.0.0
indoNLP==0.3.0

# ============================
# Visualization & Notebook
# ============================
matplotlib==3.9.0
seaborn==0.13.2
jupyter==1.0.0
ipywidgets==8.1.2

# ============================
# Model Export & Optimization
# ============================
onnx==1.16.0                          # Export via ONNX format
onnxruntime==1.18.0                   # Validate ONNX model sebelum convert

# ============================
# Utilities
# ============================
tqdm==4.66.4
python-dotenv==1.0.1
huggingface-hub==0.23.2               # Download/upload model ke HF (buat kamu!)