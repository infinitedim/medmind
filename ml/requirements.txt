# Framework utama untuk membangun, melatih, dan mengonversi model ke TFLite
tensorflow==2.21.0

# High-level API untuk membangun arsitektur model (diperlukan oleh TF 2.21.0)
keras==3.13.2

# Komputasi numerik: array operasi, matrix, preprocessing fitur tensor
numpy==2.4.2

# Manipulasi data tabular: mengolah data journal (symptom, sleep, mood) menjadi
# training dataset berstruktur sebelum dimasukkan ke model
pandas==3.0.1

# Fungsi statistik: distribusi probabilitas, uji hipotesis, validasi hasil
# korelasi dari CorrelationEngine (Pearson, Spearman, Chi-Square)
scipy==1.17.1


# -----------------------------------------------------------------------------
# Machine Learning Algorithms
# -----------------------------------------------------------------------------

# Isolation Forest untuk Anomaly Detection Model, TF-IDF Vectorizer untuk NLP
# Symptom Classifier (Opsi A: rule-based + classifier), preprocessing pipeline
scikit-learn==1.8.0

# Serialisasi model scikit-learn dan eksekusi pipeline paralel saat training
joblib==1.5.3

# Tokenisasi teks bahasa Indonesia & Inggris, stopword removal, stemming —
# preprocessing teks journal sebelum masuk ke TF-IDF symptom classifier
nltk==3.9.3

# Plot training loss, confusion matrix, distribusi data dalam notebook
matplotlib==3.10.8

# Visualisasi statistik: heatmap korelasi fitur, distribusi anomaly score
seaborn==0.13.2

# IDE notebook interaktif untuk menjalankan ketiga training notebook:
#   - anomaly_detection_training.ipynb
#   - npl_symptom_extraction_training.ipynb
#   - symptom_correlation_training.ipynb
jupyterlab==4.5.5

# Python kernel untuk JupyterLab agar notebook dapat mengeksekusi kode
ipykernel==7.2.0

# Progress bar saat iterasi training set besar dan proses konversi model
tqdm==4.67.3

# Format biner FlatBuffers — digunakan oleh TFLite untuk serialisasi model
# (.tflite files), memastikan model yang dihasilkan dapat dibaca tflite_flutter
flatbuffers==25.12.19
